import { PhotonImage } from "@silvia-odwyer/photon";
export class Change{
    //This function takes the object from fabric.Canvas's .toObject function to change it and returns it as another toObject
    changeCanvas(canvas: PhotonImage): any {

    }
    undoChange(canvas: PhotonImage): any{

    }
}